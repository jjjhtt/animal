<view class="help-cards custom-class">
	<view class="help-cards__right">
					<view class="help-cards-normal-item-container">
						<view
						  class="help-cards-normal-item-container-item"
						  wx:for="{{data}}"
						  wx:for-index="subIndex"
						  wx:key="subIndex"
						  wx:for-item="subItem"
						  bindtap="onClick"
						  data-item="{{subItem}}"
						>
							<view class="flex help-cards-normal-item-container-item-title">
								{{subItem.title}}
							</view>
              <view class="comment-container">
                <view class="name">
                  <text></text>
                </view>
                <view class="icons">
                <t-icon
                  wx:if="{{subItem.solved}}"
                  class="comment"
                  name="check-circle"
                  size="60rpx"
                  color="green"
                />
                <t-icon
                  wx:if="{{!subItem.solved}}"
                  class="comment"
                  name="error-circle"
                  size="60rpx"
                  color="red"
                />
                </view>
              </view>
						</view>
					</view>
	</view>
</view>

